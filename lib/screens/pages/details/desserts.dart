import 'package:flutter/material.dart';

class Desserts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Desserts')),
      body: Center(child: Text('Liste des desserts')),
    );
  }
}
